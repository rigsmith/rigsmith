# zizmor configuration (see the Lint workflows job in ci.yml).
rules:
  template-injection:
    ignore:
      # Generated by gh-aw from matins-watch.md and regenerated, never edited;
      # its expansions are gh-aw's own.
      - matins-watch.lock.yml
