---
type: feat
scope: clauderig
"github.com/rigsmith/rigsmith"
---

`clauderig desktop open` can now find the session for you. `-i` opens a picker over this machine's recent Claude Code sessions — no id to look up first — and `--session` completes to those same ids on `<Tab>`, each labelled with its title and project. `clauderig recent` now prints the command to reopen a session in Claude Desktop alongside the one to resume it in the terminal.
