---
type: fix
scope: codexrig
"github.com/rigsmith/rigsmith"
---

`mcp get --json` emits the same object `mcp list --json` does, and `repo status` counts a chunked rollout once at the conversation's size rather than as a pile of 4 MiB parts. A configured machine's custom folder tokens now reach path portability. The app-server client refuses further calls after a timed-out exchange rather than answering one message behind.
