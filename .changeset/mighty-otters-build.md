---
type: feat
scope: rig
"github.com/rigsmith/rigsmith"
---

New `rig stack pack` builds a member's packages from inside the stackspace, where cross-member references resolve from source — a bare checkout of a proposed branch cannot.