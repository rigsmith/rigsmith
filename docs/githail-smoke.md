# Githail smoke test

A throwaway file for exercising the Githail control plane end to end against a real pull
request: webhook → settle → gate → dispatch → reconcile → verdict.

Delete this file and close the pull request once the loop has been observed. It asserts
nothing and is not referenced anywhere.
