# Go
/bin/
/dist/
*.test
*.out
# Coverage output (reports/profiles), but never the `rig coverage` source.
coverage.*
!coverage.go
!coverage_report.go

# Local archive of pruned planning docs (purged from git history; kept on disk only)
/docs-archive/

# Editors / OS
.DS_Store
.idea/
.vscode/

# go.work is checked in for the monorepo; sums are not always needed locally
go.work.sum

# Build artifacts
**/__debug_bin*
# `go build` artifacts when run inside the installer tool dirs
/scripts/dev-install/dev-install
/scripts/source-install/source-install

# .NET build output (e.g. the examples/demo dotnet fixture)
[Bb]in/
[Oo]bj/
