// polyglot fixture — a minimal .NET console app so rig's build/run/doctor verbs
// have a real project to exercise (the go/node/cargo manifests here are
// detection-only).
Console.WriteLine("polyglot fixture: hello from .NET");
