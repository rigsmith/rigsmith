---
"@acme/utils"
type: feat
---

feat: add a debounce helper

No explicit bump on the package line, so the bump is derived from the
`type: feat` (→ minor) at version time. The browser badge shows `feat`.
